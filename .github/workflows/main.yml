on:
  push:
    branches: [ main ]
    
jobs:
  List-Branches:
    runs-on: ubuntu-latest
    name: Job para listar Branches de repositório GitHub
    steps:
      - name: Listar Branches action step
        id: list
        uses: docker://geovana10/list-branches-docker-action:v1
        with:
          owner: 'geovanams'
          repo: 'public-repo'
